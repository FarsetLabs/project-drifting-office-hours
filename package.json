{
  "name": "create-an-event-through-slack",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that books rooms on the Farset Labs Events calendar from a Slack workflow.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.80.0"
  }
}
